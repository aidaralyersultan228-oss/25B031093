nums = [1, 2, 3, 4, 5]

# 1
squares = list(map(lambda x: x*x, nums))

# 2
doubles = list(map(lambda x: x*2, nums))

# 3
strings = list(map(lambda x: str(x), nums))

# 4
negatives = list(map(lambda x: -x, nums))

# 5
plus_one = list(map(lambda x: x+1, nums))